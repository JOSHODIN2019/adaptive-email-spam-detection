[project]
name = "adaptive-spam-detection-system"
version = "0.1.0"
description = "Adaptive Email Spam Detection System - academic final-year project"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
pythonpath = ["backend"]
